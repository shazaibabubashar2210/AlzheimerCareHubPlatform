<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    <style>
        * {
            scroll-behavior: smooth;
            font-family: 'Roboto', sans-serif;
        }

        .logo {
            border-radius: 50%;
            padding: 2px 10px;
            border: 2px solid #004241;
            color: #fff;
            background-color: #004241;
        }
    </style>

</head>
<body>
    <nav class="navbar navbar-expand-lg" style="color: #ffffff">
        <div class="container-fluid">

            <a class="navbar-brand ms-5" href="#"><span class="me-2 logo">A</span>AlzhCare</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <!-- Pushes items to the right -->
                    <li class="nav-item">
                        <a class="nav-link active" asp-controller="Auth" asp-action="CaregiverDashboard">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="#">Find a Doctor</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="#">Educational Resources</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" asp-controller="AboutUs" asp-action="Index">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" asp-controller="Donation" asp-action="Donate">Donate</a>
                    </li>
                    @{
                        var userEmail = Context.Session.GetString("UserEmail");
                    }

                    @if (!string.IsNullOrEmpty(userEmail))
                    {
                        <a class="nav-link me-5 active" asp-controller="Auth" asp-action="Logout">Logout</a>
                    }
                    else
                    {
                        <a class="nav-link me-5 active" asp-controller="Auth" asp-action="Login">Login</a>
                    }


                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid mt-4">
        @RenderBody()  @* This is required *@
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

</body>
</html>